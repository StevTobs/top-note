# TOP NOTE — Product Blueprint

> แอปโน้ตที่เก็บข้อมูลในเครื่อง จัดความคิดเป็นหมวดหมู่ซ้อนกัน และใช้ AI สรุปเฉพาะส่วนที่ผู้ใช้เลือก
>
> Visual direction: Evangelion-inspired abstract — ม่วงเข้ม เขียวนีออน แผงควบคุมเชิงเรขาคณิต และพื้นที่เขียนที่อ่านสบาย

## 1. สถานะและสมมติฐาน

เอกสารนี้เป็นแบบออกแบบก่อนเริ่มพัฒนา โดยใช้ชื่อ TOP NOTE

ข้อกำหนดที่ได้รับ:

- เขียนโน้ตด้วยหัวข้อหลายระดับ ไฮไลต์ ลิงก์ และรูปภาพได้
- มีพื้นที่เก็บข้อมูลในเครื่อง
- จัดหมวดหมู่และหมวดย่อยได้
- เพิ่ม API ของ AI LLM เพื่อสร้างสรุปเฉพาะส่วนเล็ก ๆ ของโน้ต
- รูปลักษณ์แบบ Evangelion ในเชิง abstract

สิ่งที่ผู้ใช้ยืนยันเพิ่มเติม:

- เว็บแอปบนคอมพิวเตอร์เป็นแพลตฟอร์มหลัก และติดตั้งเป็น PWA ได้
- AI แสดง preview ก่อน แล้วเลือกได้ทั้งแทรกสรุปและแทนที่เฉพาะส่วนที่เลือก

สมมติฐานสำหรับรายละเอียดที่ยังไม่ได้ระบุ:

- เริ่มจากเว็บแอปสำหรับคอมพิวเตอร์ รองรับหน้าจอเล็ก และติดตั้งเป็น PWA ได้
- ไม่ต้องสมัครสมาชิก และยังไม่มีการซิงก์ข้ามอุปกรณ์ในรุ่นแรก
- การแทรกสรุปเก็บต้นฉบับไว้ ส่วนการแทนที่จะเปลี่ยนเฉพาะขอบเขตที่ preview ระบุ และย้อนกลับด้วย Undo ได้
- “Local storage” หมายถึงการเก็บข้อมูลบนอุปกรณ์ ไม่จำกัดว่าต้องใช้ browser `localStorage`
- โน้ตหนึ่งรายการอยู่ในหนึ่งหมวดหมู่ หรืออยู่ใน Inbox หากยังไม่จัดหมวดหมู่

## 2. แนวคิดของผลิตภัณฑ์

**เขียนได้ต่อเนื่อง จัดระเบียบได้ลึก และเรียก AI มาใช้กับจุดที่ต้องการ**

ประสบการณ์หลักแบ่งเป็นสามพื้นที่: เลือกหมวดหมู่ → เลือกโน้ต → เขียนและอ่าน โดยเปิดแถบ AI เฉพาะเมื่อใช้งาน เพื่อลดสิ่งรบกวนขณะจด

ตัวอย่าง:

1. สร้างหมวด `Learning / Programming / JavaScript`
2. เขียนโน้ต “Async & Await” พร้อมหัวข้อย่อย ลิงก์ และภาพประกอบ
3. เลือกข้อความสองย่อหน้าในหัวข้อ “Error Handling”
4. กด “สรุปส่วนนี้” แล้วเลือกรูปแบบ bullet
5. ตรวจผลลัพธ์ และแทรกบล็อกสรุปไว้ใต้ส่วนที่เลือก
6. หรือเลือก “แทนที่ส่วนที่เลือก” เพื่อเปลี่ยนเฉพาะข้อความต้นฉบับส่วนนั้นเป็นสรุป โดยย้อนกลับได้
7. เนื้อหาส่วนอื่นของโน้ตไม่ถูกส่งไปยัง AI และไม่ถูกแก้ไข

## 3. โครงสร้างหน้าจอ

### หน้าหลัก

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ TOP NOTE        ค้นหาโน้ต…                 บันทึกแล้ว • Offline     Settings │
├────────────────┬──────────────────┬──────────────────────────────────────────┤
│ หมวดหมู่       │ รายการโน้ต       │ Learning / Programming / JavaScript      │
│                │                  │                                          │
│ + หมวดหมู่     │ + โน้ตใหม่       │ Async & Await                            │
│ Inbox          │                  │ แก้ไขล่าสุด…                             │
│ ▼ Learning     │ Async & Await    │                                          │
│   ▼ Programming│ Promise Basics   │ H1 บทนำ                                  │
│     JavaScript │ Error Patterns   │ ข้อความ ลิงก์ และรูปภาพ                  │
│     Python     │                  │                                          │
│ ▶ Work         │                  │ H2 Error Handling                        │
│ ▶ Personal     │                  │ [ข้อความที่เลือกไว้]                     │
│                │                  │ [ไฮไลต์] [เพิ่มลิงก์] [สรุปส่วนนี้]       │
│ ถังขยะ         │                  │                                          │
└────────────────┴──────────────────┴──────────────────────────────────────────┘
```

เมื่อเรียก AI ให้เปิดแถบด้านขวา แสดงขอบเขตข้อความที่จะส่ง ตัวเลือกสรุป และผลลัพธ์ หากพื้นที่ไม่พอให้ใช้ drawer ทับพื้นที่แทนการบีบ editor จนอ่านยาก

สัดส่วนตั้งต้นสำหรับจอกว้าง:

- แถบหมวดหมู่: 220–260 px ย่อเก็บได้
- รายการโน้ต: 260–320 px ย่อเก็บได้
- พื้นที่เขียน: ขยายตามพื้นที่ โดยจำกัดความกว้างเนื้อหาประมาณ 720–820 px
- แถบ AI: 340–400 px เปิดตามการใช้งาน
- Focus mode: ซ่อนแถบหมวดหมู่และรายการโน้ต

บนมือถือใช้หน้าหมวดหมู่ รายการโน้ต และ editor แยกกัน พร้อมปุ่มย้อนกลับ; AI เปิดเต็มหน้าหรือเป็น drawer ขนาดใหญ่

### หน้าตั้งค่า

- Appearance: ธีม ขนาดตัวอักษร และลดการเคลื่อนไหว
- AI Connections: Provider, Base URL, Model และ API key
- Storage & Backup: พื้นที่ที่ใช้ สำรองข้อมูล และนำเข้าข้อมูล

## 4. Editor และโครงสร้างเนื้อหา

ใช้ editor แบบ block เพื่อระบุและประมวลผลส่วนเล็ก ๆ ได้ แต่ยังพิมพ์ต่อเนื่องเหมือนเอกสารทั่วไป

| ความสามารถ | พฤติกรรมที่ต้องการ |
|---|---|
| หัวข้อ | รองรับ H1, H2, H3 และ Paragraph; ชื่อโน้ตแยกจากหัวข้อในเนื้อหา |
| สารบัญ | สร้างจากหัวข้ออัตโนมัติ คลิกเพื่อไปยังตำแหน่งนั้น |
| ข้อความ | ตัวหนา ตัวเอียง ขีดฆ่า และ inline code |
| ไฮไลต์ | เลือกสีจากชุดจำกัด เช่น เขียว ม่วง เหลือง และล้างไฮไลต์ได้ |
| ลิงก์ | เพิ่ม แก้ไข ลบลิงก์ และวาง URL; เปิดลิงก์ผ่านการกดที่ชัดเจน |
| รูปภาพ | วางจาก clipboard ลากไฟล์ลง editor หรือเลือกไฟล์จากเครื่อง |
| ปรับภาพ | ปรับความกว้าง จัดชิดซ้าย/กึ่งกลาง ใส่คำบรรยาย และ alt text |
| รายการ | Bullet, numbered list และ checklist |
| Undo / Redo | ย้อนการแก้ไข รวมถึงการแทรกหรือแทนที่ด้วยสรุปจาก AI ในหนึ่งคำสั่ง |

คำสั่ง `/` ใช้เพิ่ม block เช่น `/heading`, `/image`, `/checklist` และ `/summary` โดยมี toolbar ให้ใช้งานได้โดยไม่ต้องจำคำสั่ง

รูปภาพต้องถูกคัดลอกเข้า storage ของแอป ไม่อ้างอิงเพียงตำแหน่งไฟล์เดิมในเครื่อง ตั้งขนาดสูงสุดต่อภาพเป็นค่าปรับได้ เช่น 10 MB และแสดงข้อความเมื่อไฟล์ใหญ่เกินกำหนด

กำหนด `blockId` ให้แต่ละ block เพื่อใช้เป็นจุดอ้างอิงของสรุป หัวข้อ และตำแหน่งแทรกผลลัพธ์

## 5. หมวดหมู่และหมวดย่อย

```text
Workspace
├── Inbox
├── Learning
│   ├── Programming
│   │   ├── JavaScript
│   │   └── Python
│   └── Languages
├── Work
│   ├── Project A
│   └── Meeting Notes
└── Personal
```

- สร้าง เปลี่ยนชื่อ และย้ายหมวดหมู่ได้
- โครงสร้างข้อมูลรองรับหลายระดับ โดยแนะนำการใช้งาน 3–5 ระดับเพื่อให้นำทางง่าย
- ย้ายโน้ตผ่านเมนู “ย้ายไป…”; การลากวางเพิ่มภายหลังได้
- ป้องกันการย้ายหมวดหมู่ไปไว้ภายในตัวเองหรือหมวดย่อยของตัวเอง
- รายการโน้ตแสดงเฉพาะหมวดที่เลือก โดยมีตัวเลือก “รวมหมวดย่อย”
- ค้นหาได้จากชื่อและข้อความในโน้ต พร้อมกรองตามหมวดหมู่
- ลบโน้ตแบบย้ายเข้าถังขยะก่อน และกู้คืนได้
- เมื่อลบหมวดหมู่ ให้เลือกระหว่างย้ายโน้ตทั้งหมดในหมวดและหมวดย่อยไป Inbox หรือย้ายทั้งชุดเข้าถังขยะ พร้อมแจ้งจำนวนรายการ
- หากกู้คืนโน้ตแล้วหมวดเดิมไม่มีอยู่ ให้ย้ายเข้า Inbox
- การลบถาวรต้องยืนยัน และไม่เกิดจากปุ่มลบปกติ

## 6. AI สรุปเฉพาะส่วน

### วิธีเลือกขอบเขต

| วิธี | ขอบเขตที่ส่งให้ AI |
|---|---|
| เลือกข้อความ | เฉพาะข้อความที่ไฮไลต์เลือกไว้ |
| เลือก block | ข้อความใน block เดียวหรือหลาย block ที่เลือก |
| เมนูหัวข้อ → สรุปส่วนนี้ | หัวข้อปัจจุบันและเนื้อหาจนถึงก่อนหัวข้อระดับเดียวกันหรือระดับสูงกว่า |

ตัวอย่าง: สรุป H2 จะรวม H3 ที่อยู่ภายใต้ H2 นั้น แต่หยุดก่อน H2 ถัดไปหรือ H1 ถัดไป

หากยังไม่มีขอบเขต ให้แจ้งให้เลือกข้อความหรือส่วนก่อน ปิดปุ่มส่งไว้ และไม่ใช้ทั้งโน้ตเป็นค่าเริ่มต้น

### ลำดับการใช้งาน

1. ผู้ใช้เลือกส่วนที่ต้องการ แล้วกด “สรุปส่วนนี้”
2. แถบ AI แสดงตัวอย่างข้อความที่จะส่ง พร้อมจำนวนตัวอักษร
3. เลือกรูปแบบ: ย่อหน้าสั้น, bullet หรือประเด็นสำคัญ
4. เลือกระดับความสั้นและภาษาของสรุป
5. กด “สร้างสรุป” เพื่อส่งคำขอ
6. ระหว่างรอแสดงสถานะ และมีปุ่มยกเลิก
7. ผลลัพธ์แสดงเป็น preview พร้อม “แทรกใต้ส่วนนี้”, “แทนที่ส่วนที่เลือก”, “คัดลอก”, “ลองใหม่” และ “ปิด”

ห้ามเขียนทับต้นฉบับอัตโนมัติ ผลลัพธ์ที่ยังไม่ได้กดแทรกหรือแทนที่จะไม่เปลี่ยนเนื้อหาโน้ต

### กติกาการแทนที่

- แสดงขอบเขตต้นฉบับและผลสรุปคู่กันใน preview ก่อนให้กดแทนที่
- เลือกบางส่วนของย่อหน้า: แทนที่เฉพาะช่วงข้อความนั้น โดยรักษาข้อความก่อนและหลังไว้ และแทรกผลเป็นข้อความปกติ
- เลือกเต็ม text blocks: แทนที่ด้วย summary block ในตำแหน่งเดิม
- สรุปผ่านเมนูหัวข้อ: รักษาหัวข้อหลักของส่วนไว้ และแทนที่เนื้อหาข้อความภายในขอบเขตที่ระบุ; preview ต้องแสดงว่าหัวข้อย่อยใดจะถูกแทนที่ด้วย
- หากขอบเขตมีรูปภาพหรือบล็อกที่ไม่ใช่ข้อความ ให้ใช้การแทรกสรุปแทน และแจ้งให้เลือกเฉพาะข้อความหากต้องการแทนที่ เพื่อไม่ลบภาพที่ AI ไม่ได้ประมวลผล
- ตรวจ bookmark และ hash ของต้นฉบับก่อนแทนที่ หากข้อความเปลี่ยนหรือขอบเขตหายไป ให้ปิดปุ่มแทนที่และให้สร้างสรุปใหม่
- การแทนที่เป็น editor transaction เดียว เพื่อ Undo คืนเนื้อหาและรูปแบบเดิมได้ในครั้งเดียวภายใน session การแก้ไข; ประวัติข้ามการเปิดแอปยังอยู่นอก MVP
- กรณีแทนที่ source เดิมจะไม่มีอยู่แล้ว ให้เก็บ metadata เป็นที่มาของสรุป ไม่แสดงเป็นลิงก์ไปยัง block ที่ถูกลบ และไม่ใช้การตรวจ stale แบบกรณีแทรก

### ขอบเขตข้อมูลและพฤติกรรม

- ส่งเฉพาะข้อความที่แสดงในตัวอย่างก่อนส่ง พร้อมคำสั่งสรุปและตัวเลือกที่ผู้ใช้กำหนด
- ไม่แนบชื่อโน้ต breadcrumb โน้ตอื่น หรือประวัติสนทนาโดยอัตโนมัติ
- รุ่นแรกสรุปข้อความเท่านั้น ไม่อัปโหลดภาพและไม่ดึงเนื้อหาปลายทางของลิงก์; ข้อความลิงก์และคำบรรยายภาพที่เลือกต้องปรากฏใน preview หากจะส่ง
- ตัดข้อมูล API key ออกจาก log และข้อความแสดงข้อผิดพลาด
- เนื้อหาโน้ตเป็นข้อมูลสำหรับสรุป ไม่ใช่คำสั่งเปลี่ยนพฤติกรรม AI; ผลลัพธ์ไม่มีสิทธิ์เรียกเครื่องมือหรือแก้เอกสารเอง
- หากขอบเขตยาวเกินขีดจำกัด ให้ขอให้เลือกส่วนเล็กลง โดยไม่ตัดข้อความทิ้งเงียบ ๆ
- หากต้นฉบับเปลี่ยนระหว่างรอ ให้แจ้งว่าผลสรุปอ้างอิงข้อความเวอร์ชันก่อนหน้าและเสนอให้สร้างใหม่
- หากตำแหน่งต้นฉบับถูกลบ ให้คง preview สำหรับคัดลอก และให้เลือกตำแหน่งแทรกใหม่
- เมื่อยกเลิก ให้หยุดรอ/ยกเลิก request ฝั่งแอปและเพิกเฉยต่อผลที่กลับมาทีหลัง; provider อาจประมวลผลไปแล้ว

### บล็อกสรุป

แสดง label `AI SUMMARY`, เนื้อหาสรุป, model และเวลาที่สร้าง โดยแก้ไขข้อความสรุปได้เหมือนเนื้อหาปกติ

สำหรับการแทรก เก็บ `sourceBlockIds` และ hash ของข้อความที่ส่ง หากต้นฉบับเปลี่ยน แสดง “ต้นฉบับมีการแก้ไข” โดยไม่สร้างสรุปใหม่เอง และไม่นำ summary block เดิมรวมเป็นต้นฉบับในการสรุปหัวข้อซ้ำ เว้นแต่ผู้ใช้เลือกบล็อกนั้นโดยตรง การแทนที่บางส่วนเป็นข้อความปกติจึงถือเป็นเนื้อหาใหม่ของโน้ตในการสรุปครั้งถัดไป

### การเชื่อมต่อ LLM

ฟอร์มตั้งค่าประกอบด้วย:

- ชื่อการเชื่อมต่อ
- ประเภท provider: ChatGPT (OpenAI), Claude (Anthropic), DeepSeek หรือ Local โดยเลือกแล้วเติม Base URL เริ่มต้นของ provider นั้นให้ทันที และแก้ไขทับได้
- Base URL
- Model: หลังกรอก API key แล้วออกจากช่อง (หรือกดปุ่ม “ดึง model อัตโนมัติ”) แอปเรียก endpoint รายชื่อ model ของ provider แล้วแสดงเป็น dropdown ให้เลือก; หากดึงไม่สำเร็จหรือ provider ไม่ส่งรายชื่อกลับมา ให้กรอก Model ID เองได้เสมอ
- API key พร้อมปุ่มแสดง/ซ่อน
- ปุ่ม “ทดสอบการเชื่อมต่อ” และ “ล้าง key”

ใช้ provider adapter แยกรูปแบบคำขอ/คำตอบของแต่ละ provider: ChatGPT, DeepSeek และ Local ใช้ OpenAI chat-completions schema (`/chat/completions`, header `Authorization: Bearer`, รายชื่อ model จาก `/models`) ส่วน Claude ใช้ Anthropic Messages API (`/v1/messages`, header `x-api-key` และ `anthropic-version`, รายชื่อ model จาก `/v1/models`) เพิ่ม provider รูปแบบอื่นได้ภายหลังผ่าน adapter เดียวกัน การทดสอบส่งข้อความทดสอบสั้นที่ไม่มีข้อมูลโน้ต และแจ้งว่าคำขออาจมีค่าใช้จ่ายตาม provider

ค่าเริ่มต้นเก็บ key ใน memory เฉพาะการใช้งานครั้งนั้น เมื่อ reload หรือปิดแอปต้องกรอกใหม่; รุ่น Desktop ในอนาคตจึงใช้ OS keychain เพื่อจำ key ได้ หลีกเลี่ยงการเก็บ key แบบข้อความธรรมดาใน `localStorage` หรือไฟล์ backup

เว็บแอปเรียก API โดยตรงได้เฉพาะ endpoint ที่อนุญาตการเรียกจาก browser ตาม CORS และนโยบายของ provider หากไม่รองรับ ต้องใช้ proxy ที่ผู้ใช้ควบคุมหรือปรับเป็น Desktop; ไม่ใช้ public proxy เพื่อผ่านข้อจำกัดนี้ และไม่สัญญาว่า Base URL ทุกแห่งจะใช้งานได้

จัดการข้อผิดพลาด key ไม่ถูกต้อง, model ไม่พบ, rate limit, timeout, ไม่มีเครือข่าย และ CORS โดยคงข้อความที่เลือกกับต้นฉบับไว้ให้ลองใหม่ ไม่ส่งซ้ำอัตโนมัติจนเกิดค่าใช้จ่ายโดยไม่ตั้งใจ

## 7. การเก็บข้อมูลและสำรองข้อมูล

### Storage ที่เสนอ

| ข้อมูล | ที่เก็บ |
|---|---|
| โน้ต หมวดหมู่ และ metadata | IndexedDB |
| รูปภาพ | Blob ใน IndexedDB แยกจากเนื้อหาเอกสาร |
| ค่าหน้าตาเล็กน้อย เช่น ย่อ sidebar | localStorage |
| API key | Memory ของ session ที่กำลังใช้งาน |
| ไฟล์สำหรับเปิดแอป offline | Service worker cache สำหรับรุ่น PWA |

IndexedDB เหมาะกับเอกสารและรูปภาพมากกว่าการเก็บทุกอย่างใน `localStorage` ซึ่งเป็นพื้นที่แบบ string และมีข้อจำกัดด้านขนาด

### การบันทึก

- บันทึกอัตโนมัติหลังหยุดพิมพ์ประมาณ 500 ms และเมื่อเกิดการเปลี่ยนแปลงโครงสร้าง
- แสดง `กำลังบันทึก…`, `บันทึกแล้ว` หรือ `บันทึกไม่สำเร็จ` ตามผลการเขียนจริง
- ใช้ revision และ transaction ป้องกันการเขียนทับข้อมูลเก่าจากหลายหน้าต่าง
- รุ่นแรกให้แท็บหนึ่งถือสิทธิ์แก้ไขโน้ต อีกแท็บเปิดอ่านได้ พร้อมตัวเลือกขอสิทธิ์แก้ไข
- ถ้าพื้นที่เต็ม ต้องแจ้งชัดเจนและให้ส่งออกเนื้อหาที่ยังค้างอยู่ได้ ไม่แสดงว่าบันทึกสำเร็จ
- อ้างอิงภาพผ่าน asset ID และล้างภาพที่ไม่มีการอ้างอิงหลังการลบถาวร โดยตรวจรวมโน้ตในถังขยะ

### Offline และการคงอยู่ของข้อมูล

สร้าง แก้ไข ค้นหา จัดหมวดหมู่ และอ่านภาพได้แบบ offline หลังโหลดและ cache ตัวแอปสำเร็จแล้ว ส่วน AI ต้องเข้าถึง endpoint ที่ตั้งค่าไว้ได้

ข้อมูลใน browser ผูกกับอุปกรณ์ browser profile และ origin การล้าง site data หรือใช้โหมดส่วนตัวอาจทำให้ข้อมูลหาย จึงต้องมี backup ที่ผู้ใช้ดาวน์โหลดเก็บเอง พร้อมแสดงวันสำรองล่าสุด และขอ persistent storage เมื่อ browser รองรับ โดยไม่รับประกันว่าจะได้รับสิทธิ์

### Backup / Import

- Backup หลักเป็น ZIP: `manifest.json`, ข้อมูลโน้ต/หมวดหมู่ และไฟล์ภาพ
- เก็บ schema version เพื่อรองรับ migration ในอนาคต
- ไม่รวม API key หรือ credentials
- Import ตรวจรูปแบบ ขนาด และความครบถ้วนของ assets ก่อนเขียนข้อมูลจริง
- ค่าเริ่มต้นนำเข้าเป็นหมวดใหม่ พร้อม remap IDs เพื่อไม่ทับโน้ตเดิม
- Export รายโน้ตเป็น Markdown พร้อมโฟลเดอร์รูปได้ แต่สีไฮไลต์และ metadata ของ AI อาจแปลงกลับได้ไม่ครบ จึงไม่ใช้แทน backup หลัก

## 8. โครงสร้างข้อมูลเบื้องต้น

```ts
type Category = {
  id: string;
  parentId: string | null;
  name: string;
  order: number;
  deletedAt: string | null;
};

type Note = {
  id: string;
  categoryId: string | null; // null = Inbox
  title: string;
  document: EditorDocument; // JSON tree ของ editor ที่เลือก
  plainText: string;        // ข้อมูลสำหรับค้นหา ไม่ใช่ต้นฉบับหลัก
  revision: number;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
};

type Asset = {
  id: string;
  blob: Blob;
  mimeType: string;
  byteSize: number;
  createdAt: string;
};

type SummaryMeta = {
  mode: 'insert' | 'replace';
  summaryBlockId: string;
  sourceBlockIds: string[];
  sourceRange: SelectionBookmark | null; // ใช้กรณีเลือกบางส่วนของ block
  sourceTextHash: string;
  sourceRevision: number;
  model: string;
  createdAt: string;
};

type AIConnection = {
  id: string;
  name: string;
  providerType: "openai" | "anthropic" | "deepseek" | "local";
  baseUrl: string;
  model: string;
  // ไม่เก็บ API key ใน record นี้
};
```

`EditorDocument` และ `SelectionBookmark` เป็นชื่อเชิงแนวคิด ต้องกำหนดตาม editor ที่เลือก โดย bookmark ต้อง remap ได้เมื่อเอกสารเปลี่ยน และต้องตรวจว่ายังใช้งานได้ก่อนแทรกสรุป

## 9. Visual Design — Evangelion Abstract

### บุคลิก

พื้นที่ทำงานเหมือนแผงควบคุมที่สงบและแม่นยำ ใช้กรอบมุมตัด เส้นบาง เลขลำดับ และจุดสถานะเป็นองค์ประกอบตกแต่ง ถ่ายทอดผ่านสีและรูปทรง โดยใช้กราฟิกที่ออกแบบขึ้นใหม่

พื้นที่เขียนควรเป็นพื้นเรียบ ส่วนลวดลายเชิงเทคนิควางไว้ที่ขอบหน้าจอ sidebar และ empty state

### Color tokens ตั้งต้น

| Token | สี | หน้าที่ |
|---|---|---|
| Background | `#0D0B14` | พื้นหลัก |
| Surface | `#171321` | Sidebar และแผงต่าง ๆ |
| Elevated | `#231B33` | เมนูและบล็อกสรุป |
| Purple | `#8B5CF6` | องค์ประกอบหลักและหมวดที่เลือก |
| Neon green | `#B7FF3C` | ปุ่ม AI, focus ring และสถานะ active |
| Amber | `#FFB547` | แจ้งเตือนและสถานะกำลังดำเนินการ |
| Red | `#FF5D73` | ข้อผิดพลาดและการลบ |
| Text primary | `#F3EFFA` | ข้อความหลัก |
| Text muted | `#B7AEC8` | ข้อมูลรอง |
| Border | `#3A2F4C` | เส้นแบ่งพื้นผิว |

ปุ่มพื้นเขียวนีออนใช้ข้อความสีเข้ม สีม่วงใช้กับพื้น selection หรือองค์ประกอบขนาดใหญ่เป็นหลัก และต้องตรวจ contrast ของคู่สีจริงก่อนส่งมอบ UI

### Typography และองค์ประกอบ

- เนื้อหา: ฟอนต์ sans-serif ที่รองรับภาษาไทย เช่น Noto Sans Thai; body เริ่มที่ 16–18 px และ line-height ประมาณ 1.7
- Metadata: monospace สำหรับตัวเลข เวลา และ label สั้น ๆ
- หัวข้อมีขนาดลดหลั่นชัดเจน ใช้เส้นหรือเลขลำดับเล็ก ๆ ประกอบได้
- Selected category: พื้นม่วงโปร่งและเส้นเขียวด้านซ้าย พร้อมสถานะที่โปรแกรมอ่านหน้าจอรับรู้ได้
- AI summary: พื้นม่วงเข้ม เส้นเขียวบาง และ label “AI SUMMARY”
- แถบสถานะใช้คำตรงความหมาย เช่น “บันทึกแล้ว” และ “ออฟไลน์”
- Empty state ใช้รูปทรงเรขาคณิตและข้อความ “เริ่มโน้ตแรกของคุณ” พร้อมปุ่มสร้างโน้ต
- Motion สั้นประมาณ 120–180 ms ปิดได้ตาม reduced-motion preference และไม่มีไฟกระพริบ

### Accessibility

- ใช้คีย์บอร์ดนำทาง toolbar หมวดหมู่ และ dialog ได้
- แสดง focus ชัดเจน และคืน focus หลังปิดเมนูหรือแถบ AI
- สีต้องมี label หรือสัญลักษณ์ช่วยอธิบายสถานะ
- ตรวจ contrast ตัวอักษรปกติอย่างน้อย 4.5:1
- รองรับการซูมข้อความและมี tooltip/accessible name ให้ปุ่มที่ใช้ไอคอน

## 10. แนวทางเทคนิค

ข้อเสนอตั้งต้นสำหรับเว็บแอป ยังไม่ใช่การเลือก dependency หรือเวอร์ชันขั้นสุดท้าย:

- React + TypeScript สำหรับ UI
- Editor บน ProseMirror เช่น Tiptap เพื่อรองรับ document tree, selection และ custom summary block
- IndexedDB ผ่านชั้น repository เช่น Dexie เพื่อรวมการบันทึก transaction และ migration
- CSS variables สำหรับ theme tokens และ responsive layout
- Provider adapter แยกการสร้างคำขอและอ่านผลจาก UI
- Service worker สำหรับเปิดแอป offline เมื่อพัฒนา PWA

```text
UI / Editor
├── Note commands ── Repository ── IndexedDB
├── Search ───────── Local text index
├── Assets ───────── Asset repository ── IndexedDB Blobs
└── Selected content snapshot
    └── AI adapter ── User-configured endpoint
        └── Preview ── User inserts / replaces selected content ── Repository
```

เริ่มพัฒนาด้วยต้นแบบเล็กเพื่อพิสูจน์การวางภาพ ภาษาไทยและการใช้ IME, selection bookmark, undo หลังแทรกสรุป และการเรียก endpoint จาก browser ก่อนสร้าง UI ทั้งระบบ

แสดงผลข้อความและผลลัพธ์ AI ผ่าน schema ของ editor ที่อนุญาตเท่านั้น กรอง HTML ที่วาง/นำเข้า และไม่อนุญาต URL scheme ที่รันสคริปต์

## 11. ขอบเขตรุ่นแรก

### MVP

- สร้าง เปลี่ยนชื่อ แก้ไข ลบ และกู้คืนโน้ต
- หมวดหมู่และหมวดย่อย พร้อมย้ายผ่านเมนู
- H1–H3, ไฮไลต์, ลิงก์, รูป และรายการ
- สารบัญและค้นหาชื่อ/ข้อความ
- บันทึกในเครื่องอัตโนมัติ พร้อมสถานะบันทึก
- เชื่อมต่อ API หนึ่งรูปแบบ และสรุปข้อความ/blocks/หัวข้อที่เลือก
- Preview พร้อมแทรก summary block หรือแทนที่เฉพาะส่วนที่เลือก และ Undo ได้
- Backup และ Import ที่รวมรูปภาพ
- ธีม Evangelion abstract พร้อม focus mode

### ระยะถัดไป

- ลากวางหมวดหมู่และโน้ต
- Tags, backlinks และ template
- ประวัติหลายเวอร์ชันของโน้ต
- Desktop พร้อม OS keychain
- Provider adapters เพิ่มเติม และสรุปรูปภาพแบบเลือกส่งอย่างชัดเจน
- การซิงก์และการทำงานร่วมกัน เมื่อมีความต้องการ

## 12. ลำดับการพัฒนาและเกณฑ์รับงาน

| ระยะ | ผลลัพธ์ | เกณฑ์รับงานหลัก |
|---|---|---|
| 1. พิสูจน์แกนหลัก | Editor + storage + API prototype | พิมพ์ไทย วางภาพ reload แล้วข้อมูลอยู่ และ endpoint ที่เลือกเรียกได้ |
| 2. พื้นที่จด | Editor เต็มและ autosave | หัวข้อ ไฮไลต์ ลิงก์ ภาพ และ undo ทำงาน; แจ้งเมื่อบันทึกล้มเหลว |
| 3. จัดหมวดหมู่ | Tree, Inbox, search, trash | ย้ายและกู้คืนข้อมูลได้ ป้องกันวงจรของหมวดหมู่ |
| 4. สรุปเฉพาะส่วน | AI settings, scope preview, summary block | payload มีเฉพาะขอบเขตที่เลือก และต้นฉบับไม่เปลี่ยนระหว่างสร้าง preview |
| 5. ความพร้อมใช้งาน | ธีม, responsive, offline, backup | ใช้งานด้วยคีย์บอร์ดได้ และนำเข้า backup ในฐานข้อมูลว่างแล้วได้ข้อความ/ภาพครบ |

สถานการณ์ที่ต้องตรวจเพิ่มเติม:

- ปิดแล้วเปิดใหม่หลังสถานะ “บันทึกแล้ว” ข้อความและรูปต้องอยู่ครบ
- สรุป H2 ที่มี H3 ต้องหยุดก่อนหัวข้อระดับเดียวกันหรือสูงกว่า
- ไม่มีข้อความที่เลือกต้องไม่ส่งทั้งโน้ต
- AI ล้มเหลวหรือผู้ใช้ยกเลิกต้องไม่เพิ่มหรือแก้เนื้อหา
- แก้ไขต้นฉบับระหว่าง AI ทำงานต้องตรวจพบ และไม่แทรกผิดตำแหน่ง
- การแทนที่ต้องเปลี่ยนเฉพาะขอบเขตที่แสดง และ Undo ครั้งเดียวคืนข้อความกับรูปแบบเดิมได้
- หากเลือกคร่อมรูปภาพ ต้องปิดการแทนที่และยังแทรกสรุปได้ โดยภาพไม่หาย
- เปิดสองแท็บต้องไม่เขียนทับการแก้ไขโดยเงียบ ๆ
- Backup ต้องไม่มี API key และ Import ต้องไม่ทับโน้ตเดิม
- พื้นที่เก็บข้อมูลเต็มต้องไม่แสดง “บันทึกแล้ว”

## 13. ประเด็นที่รอคำตอบ

1. ~~มี provider หรือ local LLM ที่ต้องรองรับเป็นพิเศษหรือไม่?~~ ตอบแล้ว: ChatGPT (OpenAI), Claude (Anthropic), DeepSeek และ Local (endpoint ที่เข้ากันได้กับ OpenAI schema เช่น Ollama/LM Studio) ดูรายละเอียด adapter ในหัวข้อ 6 “การเชื่อมต่อ LLM”
